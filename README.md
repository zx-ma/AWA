# awa

> **A**wa **W**inks **A**lice

A Linux facial authentication tool.

The name `awa` is a recursive acronym that doubles as a face kaomoji. It represents the infrared (IR) camera "winking" to authenticate the user ("Alice" in cryptography)
